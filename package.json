{
  "name": "deendirectory",
  "version": "1.0.0",
  "description": "A full-stack application for DeenDirectory",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npn install && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
